version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - chmod +x export-env.sh
        - export-env.sh
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
